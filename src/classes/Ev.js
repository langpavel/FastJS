/*
FastJs.Ev = function() {  };

// private listener array (array of function or null if unbounded)
FastJs.Ev.prototype._l = [];

FastJs.Ev.prototype.addListener = function(fn) {
	return this._l.push(fn);
};
*/

